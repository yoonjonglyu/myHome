import React from 'react';

import Layout from '../components/layout';

interface MainProps {
    
}

const Main : React.FC<MainProps> = () => {
    return(
        <Layout>
            <h1>메인</h1>
        </Layout>
    );
}

export default Main;